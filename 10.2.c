#include <stdio.h>

int main() {
    int a, b, sum;

    // Read two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Calculate sum
    sum = a + b;

    // Display result
    printf("Sum = %d\n", sum);

    return 0;
}
